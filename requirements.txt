# The Voice Agents backend SDK. `[audio]` is the microphone and speaker for
# call.py; it needs PortAudio first (macOS: brew install portaudio).
assemblyai-agents[audio] @ git+https://github.com/dan-ince-aai/assemblyai-agents-python.git@main

# The text agent.
langchain>=1.0
langchain-openai>=1.0

# Used by the weather tool.
httpx>=0.28

# Only for the tests.
pytest>=8
